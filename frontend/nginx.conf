server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://api:8010/api/;
  }

  location /stac {
    proxy_pass http://api:8010/stac;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}

